<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>EDM DLP Base64 Submit</title>
  <style>
    body {
      background-color: #121212;
      color: white;
      font-family: Arial, sans-serif;
      text-align: center;
      padding-top: 80px;
    }
    input, button {
      width: 60%;
      padding: 12px;
      font-size: 18px;
      margin: 10px;
    }
    #status {
      margin-top: 20px;
      color: #00FF00;
    }
  </style>
</head>
<body>
  <h1>EDM DLP Base64 Submit</h1>

  <input type="text" id="b64Input" placeholder="Paste base64 string here..." />
  <br />
  <button onclick="sendToWebhook()">Submit</button>
  <div id="status"></div>

  <script>
    function sendToWebhook() {
      const base64 = document.getElementById("b64Input").value;
      const webhookUrl = "https://webhook.site/YOUR-ID"; // 🔁 Replace with your actual webhook URL

      fetch(webhookUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded"
        },
        body: "data=" + encodeURIComponent(base64)
      })
      .then(response => {
        document.getElementById("status").innerText = "✅ Submitted: " + base64;
      })
      .catch(error => {
        document.getElementById("status").innerText = "❌ Error: " + error;
      });
    }
  </script>
</body>
</html>
